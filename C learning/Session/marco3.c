#include <stdio.h>
#include <conio.h>
#define Area(a) (a*a)
int main()
{
    int s1=10,aos;
    aos=Area(s1);
    printf("Using marco find area ");
    printf("area:%d",aos);
    return 0;
}